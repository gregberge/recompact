{
  "scripts": {
    "precommit": "yarn build && git add -A",
    "postcommit": "git push && git push --tags",
    "postupdate": "yarn publish lib && conventional-github-releaser --preset angular"
  }
}
